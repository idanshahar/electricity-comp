# IEC Login Integration
# Required for the "Connect IEC Account" feature.
# Generate a 32-byte random hex string, e.g.:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
IEC_COOKIE_SECRET=your_64_character_hex_string_here
